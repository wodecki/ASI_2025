# 3.py - ML Threshold Routing

```mermaid
flowchart TD
    subgraph FLOW["@flow ml_routing_pipeline()"]
        subgraph TRAIN["Train & Evaluate"]
            T1["@flow train_and_evaluate(model_1)"]
            T2["@flow train_and_evaluate(model_2)"]
            T3["@flow train_and_evaluate(model_3)"]
        end

        SELECT[Select best by F1] --> B{F1 score?}

        B -->|">= 0.85"| DEPLOY["@flow deploy_model"]
        B -->|"0.75 - 0.85"| REVIEW["@flow request_review"]
        B -->|"< 0.75"| RETRAIN["@flow trigger_retrain"]

        T1 --> SELECT
        T2 --> SELECT
        T3 --> SELECT

        DEPLOY --> R[Result]
        REVIEW --> R
        RETRAIN --> R
    end

    style FLOW fill:#e8f4f8,stroke:#0077b6
    style TRAIN fill:#f0f0f0,stroke:#999
    style T1 fill:#caf0f8,stroke:#0077b6
    style T2 fill:#caf0f8,stroke:#0077b6
    style T3 fill:#caf0f8,stroke:#0077b6
    style SELECT fill:#e2d4f0,stroke:#7b2cbf
    style B fill:#fff3cd,stroke:#ffc107
    style DEPLOY fill:#d4edda,stroke:#28a745
    style REVIEW fill:#fff3cd,stroke:#ffc107
    style RETRAIN fill:#f8d7da,stroke:#dc3545
```

## ML Routing Thresholds
```
F1 >= 0.85       -> DEPLOY to production
0.75 <= F1 < 0.85 -> REQUEST human review
F1 < 0.75        -> TRIGGER retraining
```

## Pipeline Flow
1. Train multiple models
2. Evaluate each model (accuracy, F1)
3. Select best model by F1 score
4. Route based on threshold

## Notes
- Each routing decision tracked in Prefect UI
- Compare routing outcomes across pipeline runs
- Audit trail for model deployment decisions
