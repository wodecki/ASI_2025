# 1.py - Basic If/Else Routing

```mermaid
flowchart TD
    subgraph FLOW["@flow routing_pipeline()"]
        A["@flow check_data_quality"] --> B{score >= 80%?}
        B -->|Yes| C["@flow process_valid_data"]
        B -->|No| D["@flow process_invalid_data"]
        C --> E[Result]
        D --> E
    end

    DATA[(Input Data)] --> A

    style FLOW fill:#e8f4f8,stroke:#0077b6
    style A fill:#caf0f8,stroke:#0077b6
    style B fill:#fff3cd,stroke:#ffc107
    style C fill:#d4edda,stroke:#28a745
    style D fill:#f8d7da,stroke:#dc3545
    style E fill:#90e0ef,stroke:#0077b6
```

## Routing Logic
```
if quality["is_valid"]:    # score >= 80%
    -> process_valid_data
else:
    -> process_invalid_data
```

## Notes
- Prefect tracks which branch was taken
- Each path visible in UI with execution details
- Routing history preserved across runs
