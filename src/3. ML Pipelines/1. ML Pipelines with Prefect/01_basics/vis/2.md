# 2.py - Parallel Execution with .map()

```mermaid
flowchart TD
    subgraph FLOW["@flow parallel_pipeline()"]
        A["@task load_products"] --> B[to_dict]
        B --> MAP

        subgraph MAP["process_product.map()"]
            direction LR
            P1["@task process_product(row1)"]
            P2["@task process_product(row2)"]
            P3["@task process_product(row3)"]
            PN["..."]
        end

        MAP --> R[Results List]
    end

    CSV[(products.csv)] --> A

    style FLOW fill:#e8f4f8,stroke:#0077b6
    style A fill:#caf0f8,stroke:#0077b6
    style MAP fill:#ffe8cc,stroke:#ff9f1c
    style P1 fill:#ffdd99,stroke:#ff9f1c
    style P2 fill:#ffdd99,stroke:#ff9f1c
    style P3 fill:#ffdd99,stroke:#ff9f1c
    style PN fill:#ffdd99,stroke:#ff9f1c
    style R fill:#90e0ef,stroke:#0077b6
```

## Notes
- `.map()` applies a task to each item in a list
- Tasks run in parallel (concurrent execution)
- Results are collected in order
- Each mapped task is tracked independently in UI
