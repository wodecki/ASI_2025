# 1.py - Basic Task Caching

```mermaid
flowchart TD
    subgraph FLOW["@flow caching_demo()"]
        subgraph CALL1["Call 1: expensive_computation(5, 3)"]
            C1[Compute] --> CACHE1[("Cache
            key: (5,3)")]
        end

        subgraph CALL2["Call 2: expensive_computation(5, 3)"]
            CACHE1 -.->|"Cache HIT"| R2[Return cached]
        end

        subgraph CALL3["Call 3: expensive_computation(10, 20)"]
            C3[Compute] --> CACHE2[("Cache
            key: (10,20)")]
        end

        CALL1 --> CALL2
        CALL2 --> CALL3
    end

    style FLOW fill:#e8f4f8,stroke:#0077b6
    style CALL1 fill:#ffe8cc,stroke:#ff9f1c
    style CALL2 fill:#d4edda,stroke:#28a745
    style CALL3 fill:#ffe8cc,stroke:#ff9f1c
    style C1 fill:#ffdd99,stroke:#ff9f1c
    style C3 fill:#ffdd99,stroke:#ff9f1c
    style R2 fill:#c8e6c9,stroke:#28a745
    style CACHE1 fill:#e2d4f0,stroke:#7b2cbf
    style CACHE2 fill:#e2d4f0,stroke:#7b2cbf
```

## Cache Behavior
| Call | Inputs | Action | Time |
|------|--------|--------|------|
| 1 | (5, 3) | Compute | 2s |
| 2 | (5, 3) | Cache hit | ~0s |
| 3 | (10, 20) | Compute (new key) | 2s |

## Configuration
```python
from prefect.cache_policies import INPUTS

@task(cache_policy=INPUTS)
def expensive_computation(x, y):
    # Cached by input values
    return x + y
```

## Notes
- `INPUTS` policy caches by function arguments
- Same inputs = cache hit (instant return)
- Different inputs = new computation
