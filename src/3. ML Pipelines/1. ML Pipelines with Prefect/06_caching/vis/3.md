# 3.py - Cache Refresh Pattern

```mermaid
flowchart TD
    subgraph FLOW["@flow training_pipeline(config, force_retrain)"]
        FEAT["@task compute_features"] --> CHECK{force_retrain?}

        CHECK -->|"False"| NORMAL["train_model(config)"]
        CHECK -->|"True"| REFRESH["train_model.with_options(
            refresh_cache=True
        )(config)"]

        NORMAL --> CACHE{Cache exists?}
        CACHE -->|"Yes"| HIT[Return cached model]
        CACHE -->|"No"| TRAIN1[Train & cache]

        REFRESH --> TRAIN2[Train & update cache]
    end

    style FLOW fill:#e8f4f8,stroke:#0077b6
    style FEAT fill:#caf0f8,stroke:#0077b6
    style CHECK fill:#fff3cd,stroke:#ffc107
    style NORMAL fill:#caf0f8,stroke:#0077b6
    style REFRESH fill:#f8d7da,stroke:#dc3545
    style HIT fill:#d4edda,stroke:#28a745
    style TRAIN1 fill:#ffe8cc,stroke:#ff9f1c
    style TRAIN2 fill:#ffe8cc,stroke:#ff9f1c
```

## Execution Timeline
```mermaid
sequenceDiagram
    participant User
    participant Pipeline
    participant Cache

    User->>Pipeline: Run 1 (force=False)
    Pipeline->>Cache: Check cache
    Cache-->>Pipeline: Miss
    Pipeline->>Pipeline: Train model (2s)
    Pipeline->>Cache: Store result

    User->>Pipeline: Run 2 (force=False)
    Pipeline->>Cache: Check cache
    Cache-->>Pipeline: Hit!
    Note right of Pipeline: Instant return

    User->>Pipeline: Run 3 (force=True)
    Note right of Pipeline: Bypass cache
    Pipeline->>Pipeline: Train model (2s)
    Pipeline->>Cache: Update result
```

## Force Refresh Pattern
```python
# Normal call - uses cache if available
model = train_model(config)

# Force refresh - bypasses cache
model = train_model.with_options(refresh_cache=True)(config)
```

## Use Cases
- **Retraining**: Force new model with same config
- **Data refresh**: Get fresh data despite valid cache
- **Debugging**: Bypass cache to test changes
- **Scheduled updates**: Periodic cache refresh
