# 3.py - Fallback Patterns

```mermaid
flowchart TD
    subgraph FLOW["@flow fetch_with_fallback()"]
        subgraph PRIMARY["Try: @task primary_api (retries=2)"]
            P1[Attempt 1] -->|Fail| P2[Attempt 2]
            P2 -->|Fail| P3[Attempt 3]
            P3 -->|Fail| P_FAIL[Exhausted]
        end

        subgraph BACKUP["Catch: @task backup_api (retries=1)"]
            B1[Attempt 1] -->|Fail| B2[Attempt 2]
            B2 -->|Fail| B_FAIL[Exhausted]
        end

        subgraph CACHE["Catch: @task use_cached_data"]
            C1[Return cached data]
        end

        P_FAIL -->|"except"| BACKUP
        B_FAIL -->|"except"| CACHE
    end

    R[Result]
    PRIMARY -->|Success| R
    BACKUP -->|Success| R
    CACHE --> R

    style FLOW fill:#e8f4f8,stroke:#0077b6
    style PRIMARY fill:#d4edda,stroke:#28a745
    style BACKUP fill:#fff3cd,stroke:#ffc107
    style CACHE fill:#f8d7da,stroke:#dc3545
    style P_FAIL fill:#f8d7da,stroke:#dc3545
    style B_FAIL fill:#f8d7da,stroke:#dc3545
    style C1 fill:#e2d4f0,stroke:#7b2cbf
```

## Cascading Fallback Pattern
```python
@flow
def fetch_with_fallback(endpoint):
    try:
        return primary_api(endpoint)    # Fast, unreliable
    except:
        pass

    try:
        return backup_api(endpoint)     # Slower, reliable
    except:
        pass

    return use_cached_data()            # Last resort
```

## Fallback Hierarchy
| Priority | Source | Reliability | Speed |
|----------|--------|-------------|-------|
| 1 | Primary API | Low (80% fail) | Fast |
| 2 | Backup API | High (20% fail) | Medium |
| 3 | Cached Data | 100% | Instant |

## Notes
- Each level has its own retry configuration
- Graceful degradation when all APIs fail
- Prefect tracks which fallback was used
