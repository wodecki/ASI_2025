# 2.py - Exponential Backoff

```mermaid
flowchart LR
    subgraph FIXED["Fixed Delay (2s)"]
        direction LR
        F1[Attempt 1] -->|"wait 2s"| F2[Attempt 2]
        F2 -->|"wait 2s"| F3[Attempt 3]
        F3 -->|"wait 2s"| F4[Attempt 4]
    end

    style F1 fill:#f8d7da,stroke:#dc3545
    style F2 fill:#f8d7da,stroke:#dc3545
    style F3 fill:#f8d7da,stroke:#dc3545
    style F4 fill:#d4edda,stroke:#28a745
```

```mermaid
flowchart LR
    subgraph EXPO["Exponential Backoff (1s, 5s, 30s)"]
        direction LR
        E1[Attempt 1] -->|"wait 1s"| E2[Attempt 2]
        E2 -->|"wait 5s"| E3[Attempt 3]
        E3 -->|"wait 30s"| E4[Attempt 4]
    end

    style E1 fill:#f8d7da,stroke:#dc3545
    style E2 fill:#f8d7da,stroke:#dc3545
    style E3 fill:#f8d7da,stroke:#dc3545
    style E4 fill:#d4edda,stroke:#28a745
```

## Delay Patterns

| Pattern | Config | Use Case |
|---------|--------|----------|
| Fixed | `retry_delay_seconds=2` | Simple retry |
| Exponential | `retry_delay_seconds=[1, 5, 30]` | Rate limits, backpressure |

## Configuration
```python
# Fixed delay
@task(retries=3, retry_delay_seconds=2)

# Exponential backoff
@task(retries=3, retry_delay_seconds=[1, 5, 30])
```

## Notes
- Exponential backoff ideal for API rate limits (429)
- Gives external services time to recover
- Prefect handles all timing automatically
